# Tests

Test directories will be added by family after the feasibility spike.

The detector must never read fixture ground truth. Fixture ground truth exists only for evaluation of recorder fidelity and fault discriminability.
